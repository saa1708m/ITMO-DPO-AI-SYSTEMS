# Тест 5.1. CI/CD для ML

## Цель

Проверить понимание основных принципов CI/CD в ML-проектах, структуры типичного конвейера, различий между DevOps и MLOps, а также назначения линтеров, тестов, сборки и публикации Docker-образов.

## Инструкция

Выберите один правильный ответ в каждом задании.

## Задания

1. Какое из утверждений **наиболее точно** отражает отличие MLOps от классического DevOps?
   1. MLOps используется только для фронтенд-разработки.
   2. MLOps учитывает не только код приложения, но и данные, модели, качество предсказаний и цикл переобучения.
   3. MLOps не предполагает автоматизацию.
   4. MLOps полностью исключает контейнеризацию.
   5. MLOps относится только к управлению требованиями.

2. Какая последовательность шагов наиболее типична для базового CI-конвейера ML-сервиса?
   1. Публикация образа → линтинг → тестирование → сборка.
   2. Линтинг → тестирование → сборка образа → публикация артефакта.
   3. Обучение модели → A/B-тестирование → аудит безопасности.
   4. Оркестрация DAG → сборка образа → настройка RBAC.
   5. Мониторинг → анализ дрейфа → деплой.

3. Для чего в CI-конвейере обычно используются линтеры (`ruff`, `flake8`)?
   1. Для сжатия Docker-образа.
   2. Для автоматического улучшения метрик модели.
   3. Для проверки качества оформления и синтаксической корректности кода.
   4. Для хранения данных обучения.
   5. Для публикации артефактов в registry.

4. Что является типичным результатом шага `build` в CI-конвейере ML-сервиса?
   1. Готовый dashboard в Grafana.
   2. Собранный Docker-образ с приложением и зависимостями.
   3. Размеченный датасет.
   4. Архитектурная диаграмма.
   5. DAG в Airflow.

5. Какой из инструментов чаще всего используется для модульного тестирования Python-кода в ML-проекте?
   1. pytest
   2. Grafana
   3. MinIO
   4. Kubernetes
   5. Vault

6. Зачем публиковать Docker-образ в registry?
   1. Чтобы хранить результаты опросов студентов.
   2. Чтобы использовать образ в следующих этапах доставки и развёртывания.
   3. Чтобы заменить систему контроля версий.
   4. Чтобы запускать линтеры.
   5. Чтобы автоматически переобучать модель.

7. Какой вариант тегирования Docker-образов является более осмысленным в CI?
   1. Использовать только тег `temp`.
   2. Не использовать теги вообще.
   3. Использовать тег по commit SHA и отдельный тег для основной ветки.
   4. Каждый раз случайно генерировать имя вручную.
   5. Всегда использовать только `draft`.

8. Что из перечисленного лучше всего относится к проверкам качества ML-системы в CI?
   1. Проверка схемы данных, unit-тесты и smoke-тест контейнера.
   2. Только ручная защита проекта.
   3. Только презентация для заказчика.
   4. Только запуск ноутбука Jupyter.
   5. Только публикация README.

9. Какова основная цель этапа Continuous Integration?
   1. Отложить проверку кода до экзамена.
   2. Регулярно автоматически проверять изменения при их внесении в репозиторий.
   3. Исключить тестирование.
   4. Заменить разработчиков.
   5. Отказаться от ветвления в Git.

10. Что является хорошей практикой для CI в ML-проекте?
   1. Хранить секреты прямо в файле workflow.
   2. Пропускать тесты ради ускорения сборки.
   3. Автоматизировать повторяемые проверки и минимизировать ручные операции.
   4. Публиковать каждый эксперимент сразу в production.
   5. Не фиксировать версии зависимостей.

## Ключ

1 — 2  
2 — 2  
3 — 3  
4 — 2  
5 — 1  
6 — 2  
7 — 3  
8 — 1  
9 — 2  
10 — 3
